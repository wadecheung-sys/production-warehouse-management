# OS
.DS_Store
Thumbs.db

# IDE
.vscode/*
!.vscode/extensions.json
.idea

# Local env overrides
.env.local
.env.*.local

# Reference materials (local only, not for version control)
reference/

# Local workspace — plans, progress, session notes (not for version control)
workspace/
